# Sample App

This is a minimal, Railway-ready sample application.

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python sample-app/src/app.py`

## Deployment
This app is configured to run on port 8080, suitable for Railway deployment.