# Phase 4 Sample App

This is a minimal, Railway-ready application.

## Setup
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the application: `python app.py`

## Deployment
This app is configured for seamless deployment on Railway. Connect your Railway service and set the build command to `python app.py`.